// document.addEventListener('DOMContentLoaded', () => {
//   const placeholders = document.querySelectorAll('.youtube-placeholder');

//   placeholders.forEach(placeholder => {
//       const button = placeholder.querySelector('.popup-button');

//       // Ajuster la taille de la police en fonction de la largeur du conteneur
//       const adjustFontSize = () => {
//           const containerWidth = placeholder.offsetWidth; // Largeur réelle du conteneur
//           const fontSize = containerWidth * 0.02; // Taille de la police : 2% de la largeur
//           button.style.fontSize = `${fontSize}px`;
//       };

//       adjustFontSize(); // Ajuste initialement
//       window.addEventListener('resize', adjustFontSize); // Ajuste lors des redimensionnements
//   });
// });
